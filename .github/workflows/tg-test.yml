name: Telegram Test
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Telegram
        run: |
          pip install requests -q
          python3 -c '
import requests
TOKEN = "8953203074:AAFoC_x9cxWq4KhniNHwpnKOVZXdw2vRrB4"
CHAT_ID = "2055780815"
r = requests.post(f"https://api.telegram.org/bot{TOKEN}/sendMessage", json={
    "chat_id": CHAT_ID,
    "text": "✅ *Bot 3 XAU Scalper — Telegram Bağlantı Testi*\n\nBu mesajı görüyorsan bot düzgün çalışıyor!",
    "parse_mode": "Markdown"
}, timeout=15)
print(f"Status: {r.status_code}")
print(r.json())
'
